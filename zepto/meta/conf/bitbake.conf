TMPDIR = "${TOPDIR}/tmp"
CACHE = "${TMPDIR}/cache"
STAMP = "${TMPDIR}/stamps/${PN}"
T = "${TMPDIR}/work"
B = "${TMPDIR}"
DLDIR = "${TOPDIR}/../dl"

BB_NUMBER_THREADS = "2"
ROOTFS = "${TMPDIR}/rootfs"
